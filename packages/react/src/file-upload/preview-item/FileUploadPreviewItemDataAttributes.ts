export enum FileUploadPreviewItemDataAttributes {
  /**
   * Present when the file upload is in progress.
   */
  uploading = 'data-uploading',
  /**
   * Present when the file upload is complete.
   */
  complete = 'data-complete',
}
