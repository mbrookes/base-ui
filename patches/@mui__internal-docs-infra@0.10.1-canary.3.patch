diff --git a/pipeline/loadServerTypesMeta/formatType.mjs b/pipeline/loadServerTypesMeta/formatType.mjs
--- a/pipeline/loadServerTypesMeta/formatType.mjs
+++ b/pipeline/loadServerTypesMeta/formatType.mjs
@@ -426,7 +426,7 @@
   const flatName = typeName.namespaces && typeName.namespaces.length > 0 ? typeName.namespaces.join('') + typeName.name : typeName.name;
 
   // Check if this type is in our map (exact match)
-  if (typeNameMap[flatName]) {
+  if (Object.prototype.hasOwnProperty.call(typeNameMap, flatName) && typeNameMap[flatName]) {
     // This is one of our component types - use the mapped dotted name
     const typeArgsStart = nameWithTypeArgs.indexOf('<');
     if (typeArgsStart !== -1) {
@@ -453,7 +453,7 @@
   // This check comes BEFORE flat prefix matching to preserve namespace structure
   if (typeName.namespaces && typeName.namespaces.length > 0) {
     // Check if any namespace part is in the typeNameMap
-    const transformedNamespaces = typeName.namespaces.map(ns => typeNameMap[ns] || ns);
+    const transformedNamespaces = typeName.namespaces.map(ns => (Object.prototype.hasOwnProperty.call(typeNameMap, ns) ? typeNameMap[ns] : null) || ns);
     const hasTransformation = transformedNamespaces.some((ns, i) => ns !== typeName.namespaces[i]);
     if (hasTransformation) {
       // Build the transformed name: TransformedNamespace.Member
diff --git a/pipeline/loadServerTypes/extractTypeProps.mjs b/pipeline/loadServerTypes/extractTypeProps.mjs
index 3b40fc91bad4689b6121cac56e762052a4c3ea99..d17ecdb105d3ecccc30c2773fb75e389868558f7 100644
--- a/pipeline/loadServerTypes/extractTypeProps.mjs
+++ b/pipeline/loadServerTypes/extractTypeProps.mjs
@@ -546,7 +546,7 @@ export function extractTypeProps(hast) {
     frame,
     code
   } = result;
-  const accumulators = {};
+  const accumulators = Object.create(null);
 
   // Track JSDoc comment accumulation
   let pendingCommentTexts = null;
diff --git a/pipeline/loadServerTypes/highlightTypes.mjs b/pipeline/loadServerTypes/highlightTypes.mjs
index 8da145ed96e47241e0decc09f6107a8694ffe3dc..43eab52695d25257f751b8a598798e43e0750e68 100644
--- a/pipeline/loadServerTypes/highlightTypes.mjs
+++ b/pipeline/loadServerTypes/highlightTypes.mjs
@@ -82,7 +82,7 @@ export async function highlightTypes(types, externalTypes = {}, output = 'hast',
  * @returns Map of export names (e.g., "Checkbox.Root.Props") to highlighted HAST
  */
 async function buildHighlightedExports(types, externalTypes = {}) {
-  const exports = {};
+  const exports = Object.create(null);
   if (types.length === 0) {
     // Still add external types even if no types
     await Promise.all(Object.entries(externalTypes).map(async ([typeName, definition]) => {
