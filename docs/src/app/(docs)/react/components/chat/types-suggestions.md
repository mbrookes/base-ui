# Chat

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/react/components/chat/types-suggestions.md"'

## API Reference

### Root

Renders a group of suggestion buttons. Hidden when the conversation has messages
unless `alwaysVisible` is set.
Renders a `<div>` element with `role="group"`.

Documentation: [Base UI Chat](https://base-ui.com/react/components/chat)

**Root Props:**

| Prop          | Type                                                                                               | Default | Description                                                                                                                                                                                   |
| :------------ | :------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| alwaysVisible | `boolean`                                                                                          | `false` | When `true`, renders suggestions even if the conversation has messages.                                                                                                                       |
| autoSubmit    | `boolean`                                                                                          | `false` | Whether to auto-submit when a suggestion is clicked.                                                                                                                                          |
| suggestions   | `(string \| ChatSuggestion)[]`                                                                     | -       | Suggestion items. Strings are normalized to `{ value, label }`.&#xA;Ignored when `children` are provided.                                                                                     |
| className     | `string \| ((state: ChatSuggestions.Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style         | `React.CSSProperties \| ((state: ChatSuggestions.Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render        | `ReactElement \| ((props: HTMLProps, state: ChatSuggestions.Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Root.Props

Re-export of [Root](#root) props.

### Root.State

```typescript
type ChatSuggestionsRootState = { empty: boolean; suggestionCount: number };
```

### Item

A single suggestion button that pre-fills the composer when clicked.
Renders a `<button>` element.

Documentation: [Base UI Chat](https://base-ui.com/react/components/chat)

**Item Props:**

| Prop      | Type                                                                                               | Default | Description                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| label     | `string`                                                                                           | -       | Display label. Falls back to `value`.                                                                                                                                                         |
| value\*   | `string`                                                                                           | -       | The value to pre-fill into the composer when clicked.                                                                                                                                         |
| onClick   | `React.MouseEventHandler<HTMLButtonElement>`                                                       | -       | -                                                                                                                                                                                             |
| index     | `number`                                                                                           | -       | The index of this item within the suggestions list.                                                                                                                                           |
| children  | `React.ReactNode`                                                                                  | -       | -                                                                                                                                                                                             |
| className | `string \| ((state: ChatSuggestions.Item.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatSuggestions.Item.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatSuggestions.Item.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Item.Props

Re-export of [Item](#item) props.

### Item.State

```typescript
type ChatSuggestionsItemState = { value: string; label: string; index: number };
```

### ChatSuggestionsItem

A single suggestion button that pre-fills the composer when clicked.
Renders a `<button>` element.

Documentation: [Base UI Chat](https://base-ui.com/react/components/chat)

**ChatSuggestionsItem Props:**

| Prop      | Type                                                                                               | Default | Description                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| label     | `string`                                                                                           | -       | Display label. Falls back to `value`.                                                                                                                                                         |
| value\*   | `string`                                                                                           | -       | The value to pre-fill into the composer when clicked.                                                                                                                                         |
| onClick   | `React.MouseEventHandler<HTMLButtonElement>`                                                       | -       | -                                                                                                                                                                                             |
| index     | `number`                                                                                           | -       | The index of this item within the suggestions list.                                                                                                                                           |
| children  | `React.ReactNode`                                                                                  | -       | -                                                                                                                                                                                             |
| className | `string \| ((state: ChatSuggestions.Item.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatSuggestions.Item.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatSuggestions.Item.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatSuggestionsItem.Props

```typescript
type ChatSuggestionsItemProps = {
  /** The value to pre-fill into the composer when clicked. */
  value: string;
  /** Display label. Falls back to `value`. */
  label?: string;
  /** The index of this item within the suggestions list. */
  index?: number;
  children?: React.ReactNode;
  onClick?: React.MouseEventHandler<HTMLButtonElement>;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatSuggestions.Item.State) => string | undefined);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatSuggestions.Item.State) => React.CSSProperties | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatSuggestions.Item.State) => ReactElement);
};
```

### ChatSuggestionsItem.State

```typescript
type ChatSuggestionsItemState = { value: string; label: string; index: number };
```

### ChatSuggestionsRoot

Renders a group of suggestion buttons. Hidden when the conversation has messages
unless `alwaysVisible` is set.
Renders a `<div>` element with `role="group"`.

Documentation: [Base UI Chat](https://base-ui.com/react/components/chat)

**ChatSuggestionsRoot Props:**

| Prop          | Type                                                                                               | Default | Description                                                                                                                                                                                   |
| :------------ | :------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| alwaysVisible | `boolean`                                                                                          | `false` | When `true`, renders suggestions even if the conversation has messages.                                                                                                                       |
| autoSubmit    | `boolean`                                                                                          | `false` | Whether to auto-submit when a suggestion is clicked.                                                                                                                                          |
| suggestions   | `(string \| ChatSuggestion)[]`                                                                     | -       | Suggestion items. Strings are normalized to `{ value, label }`.&#xA;Ignored when `children` are provided.                                                                                     |
| className     | `string \| ((state: ChatSuggestions.Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style         | `React.CSSProperties \| ((state: ChatSuggestions.Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render        | `ReactElement \| ((props: HTMLProps, state: ChatSuggestions.Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatSuggestionsRoot.Props

```typescript
type ChatSuggestionsRootProps = {
  /**
   * Suggestion items. Strings are normalized to `{ value, label }`.
   * Ignored when `children` are provided.
   */
  suggestions?: (string | ChatSuggestion)[];
  /**
   * Whether to auto-submit when a suggestion is clicked.
   * @default false
   */
  autoSubmit?: boolean;
  /**
   * When `true`, renders suggestions even if the conversation has messages.
   * @default false
   */
  alwaysVisible?: boolean;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatSuggestions.Root.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatSuggestions.Root.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatSuggestions.Root.State) => React.CSSProperties | undefined);
};
```

### ChatSuggestionsRoot.State

```typescript
type ChatSuggestionsRootState = { empty: boolean; suggestionCount: number };
```

## Additional Types

### ChatSuggestions.ChatSuggestion

```typescript
type ChatSuggestionsChatSuggestion = {
  /** The value to pre-fill into the composer when the suggestion is clicked. */
  value: string;
  /** Display label. Falls back to `value` if omitted. */
  label?: string;
};
```

## Export Groups

- `ChatSuggestions.Root`: `ChatSuggestions.Root`, `ChatSuggestions.Root.State`, `ChatSuggestions.Root.Props`
- `ChatSuggestions.Item`: `ChatSuggestions.Item`, `ChatSuggestions.Item.State`, `ChatSuggestions.Item.Props`
- `Default`: `ChatSuggestions.ChatSuggestion`, `ChatSuggestionsRoot`, `ChatSuggestionsRoot.State`, `ChatSuggestionsRoot.Props`, `ChatSuggestionsItem`, `ChatSuggestionsItem.State`, `ChatSuggestionsItem.Props`, `ChatSuggestion`

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `ChatSuggestions.Root`: `ChatSuggestionsRoot`
- `ChatSuggestions.Item`: `ChatSuggestionsItem`
