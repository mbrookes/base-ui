# Chat

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/react/components/chat/types-message-list.md"'

## API Reference

### Root

The root component of the chat message list. Manages scroll behavior,
auto-scrolling, roving focus, and provides context to child parts.
Renders a `<div>` element.

**Root Props:**

| Prop              | Type                                                                                               | Default | Description                                                                                                                                                                                    |
| :---------------- | :------------------------------------------------------------------------------------------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| autoScroll        | `boolean \| MessageListRootAutoScrollConfig`                                                       | `true`  | Controls automatic scrolling to the bottom when new messages arrive. `true` – enable with the default buffer (150 px).`{ buffer: number }` – enable with a custom threshold.`false` – disable. |
| enableRovingFocus | `boolean`                                                                                          | `true`  | Whether the message list manages a roving tabindex over its messages.                                                                                                                          |
| estimatedItemSize | `number`                                                                                           | -       | -                                                                                                                                                                                              |
| getItemKey        | `((id: string, index: number) => React.Key)`                                                       | -       | -                                                                                                                                                                                              |
| items             | `string[]`                                                                                         | -       | Override the list of message IDs to render. Defaults to the IDs from the chat store.                                                                                                           |
| onReachBottom     | `(() => void)`                                                                                     | -       | -                                                                                                                                                                                              |
| onReachTop        | `(() => void)`                                                                                     | -       | -                                                                                                                                                                                              |
| renderItem\*      | `ChatMessageList.Root.renderItem`                                                                  | -       | Renders each message row. Required.                                                                                                                                                            |
| className         | `string \| ((state: ChatMessageList.Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                       |
| style             | `React.CSSProperties \| ((state: ChatMessageList.Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                    |
| render            | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render.  |

### Root.Props

Re-export of [Root](#root) props.

### Root.State

```typescript
type ChatMessageListRootState = { messageCount: number; atBottom: boolean };
```

### Content

The content container of the chat message list.
Renders the list of message rows.
Renders a `<div>` element.

**Content Props:**

| Prop      | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageList.Content.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageList.Content.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Content.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Content.Props

Re-export of [Content](#content) props.

### Content.State

```typescript
type ChatMessageListContentState = { [key: string]: never };
```

### Viewport

The scrollable viewport of the chat message list.
Bind your scroll container to this element.
Renders a `<div>` element.

**Viewport Props:**

| Prop      | Type                                                                                                   | Default | Description                                                                                                                                                                                   |
| :-------- | :----------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageList.Viewport.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageList.Viewport.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Viewport.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Viewport.Props

Re-export of [Viewport](#viewport) props.

### Viewport.State

```typescript
type ChatMessageListViewportState = { [key: string]: never };
```

### Status

A visually-hidden `role="status"` live region that announces streaming
transitions ("Assistant is responding" / "Response complete") to screen readers.
Renders a `<div>` element.

**Status Props:**

| Prop      | Type                                                                                                 | Default | Description                                                                                                                                                                                   |
| :-------- | :--------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageList.Status.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageList.Status.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Status.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Status.Props

Re-export of [Status](#status) props.

### Status.State

```typescript
type ChatMessageListStatusState = { [key: string]: never };
```

### ChatMessageListContent

The content container of the chat message list.
Renders the list of message rows.
Renders a `<div>` element.

**ChatMessageListContent Props:**

| Prop      | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageList.Content.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageList.Content.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Content.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatMessageListContent.Props

```typescript
type ChatMessageListContentProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatMessageList.Content.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatMessageList.Content.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatMessageList.Content.State) => React.CSSProperties | undefined);
};
```

### ChatMessageListContent.State

```typescript
type ChatMessageListContentState = { [key: string]: never };
```

### ChatMessageListDateDivider

A date separator rendered between messages that span a calendar day boundary.
Returns `null` when there is no boundary for the current message.
Renders a `<div>` element.

**ChatMessageListDateDivider Props:**

| Prop              | Type                                                                                                                                                   | Default | Description                                                                                                                                                                                   |
| :---------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| formatDate        | `((date: Date) => React.ReactNode)`                                                                                                                    | -       | -                                                                                                                                                                                             |
| index             | `number`                                                                                                                                               | -       | -                                                                                                                                                                                             |
| items             | `string[]`                                                                                                                                             | -       | -                                                                                                                                                                                             |
| messageId\*       | `string`                                                                                                                                               | -       | -                                                                                                                                                                                             |
| shouldShowDivider | `((params: { message: ChatMessage; previousMessage: ChatMessage \| null; index: number; date: Date \| null; previousDate: Date \| null }) => boolean)` | -       | -                                                                                                                                                                                             |
| className         | `string \| ((state: ChatMessageList.DateDivider.State) => string \| undefined)`                                                                        | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style             | `React.CSSProperties \| ((state: ChatMessageList.DateDivider.State) => React.CSSProperties \| undefined)`                                              | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render            | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.DateDivider.State) => ReactElement)`                                                       | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatMessageListDateDivider.Props

```typescript
type ChatMessageListDateDividerProps = {
  messageId: string;
  index?: number;
  items?: string[];
  formatDate?: (date: Date) => React.ReactNode;
  shouldShowDivider?: (params: {
    message: ChatMessage;
    previousMessage: ChatMessage | null;
    index: number;
    date: Date | null;
    previousDate: Date | null;
  }) => boolean;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatMessageList.DateDivider.State) => string | undefined);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatMessageList.DateDivider.State) => React.CSSProperties | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatMessageList.DateDivider.State) => ReactElement);
};
```

### ChatMessageListDateDivider.State

```typescript
type ChatMessageListDateDividerState = {
  messageId: string;
  hasBoundary: boolean;
  label: React.ReactNode;
};
```

### ChatMessageListOverlay

A floating layer rendered above the message list, anchored to its bottom edge.
Pointer events pass through to the list below.
Renders a `<div>` element.

**ChatMessageListOverlay Props:**

| Prop      | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageList.Overlay.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageList.Overlay.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Overlay.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatMessageListOverlay.Props

```typescript
type ChatMessageListOverlayProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatMessageList.Overlay.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatMessageList.Overlay.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatMessageList.Overlay.State) => React.CSSProperties | undefined);
};
```

### ChatMessageListOverlay.State

```typescript
type ChatMessageListOverlayState = { [key: string]: never };
```

### ChatMessageListRoot

The root component of the chat message list. Manages scroll behavior,
auto-scrolling, roving focus, and provides context to child parts.
Renders a `<div>` element.

**ChatMessageListRoot Props:**

| Prop              | Type                                                                                               | Default | Description                                                                                                                                                                                    |
| :---------------- | :------------------------------------------------------------------------------------------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| autoScroll        | `boolean \| MessageListRootAutoScrollConfig`                                                       | `true`  | Controls automatic scrolling to the bottom when new messages arrive. `true` – enable with the default buffer (150 px).`{ buffer: number }` – enable with a custom threshold.`false` – disable. |
| enableRovingFocus | `boolean`                                                                                          | `true`  | Whether the message list manages a roving tabindex over its messages.                                                                                                                          |
| estimatedItemSize | `number`                                                                                           | -       | -                                                                                                                                                                                              |
| getItemKey        | `((id: string, index: number) => React.Key)`                                                       | -       | -                                                                                                                                                                                              |
| items             | `string[]`                                                                                         | -       | Override the list of message IDs to render. Defaults to the IDs from the chat store.                                                                                                           |
| onReachBottom     | `(() => void)`                                                                                     | -       | -                                                                                                                                                                                              |
| onReachTop        | `(() => void)`                                                                                     | -       | -                                                                                                                                                                                              |
| renderItem\*      | `ChatMessageList.Root.renderItem`                                                                  | -       | Renders each message row. Required.                                                                                                                                                            |
| className         | `string \| ((state: ChatMessageList.Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                       |
| style             | `React.CSSProperties \| ((state: ChatMessageList.Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                    |
| render            | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render.  |

### ChatMessageListRoot.Props

```typescript
type ChatMessageListRootProps = {
  /** Override the list of message IDs to render. Defaults to the IDs from the chat store. */
  items?: string[];
  /** Renders each message row. Required. */
  renderItem: ChatMessageList.Root.renderItem;
  getItemKey?: (id: string, index: number) => React.Key;
  estimatedItemSize?: number;
  onReachTop?: () => void;
  onReachBottom?: () => void;
  /**
   * Controls automatic scrolling to the bottom when new messages arrive.
   * - `true` – enable with the default buffer (150 px).
   * - `{ buffer: number }` – enable with a custom threshold.
   * - `false` – disable.
   * @default true
   */
  autoScroll?: boolean | MessageListRootAutoScrollConfig;
  /**
   * Whether the message list manages a roving tabindex over its messages.
   * @default true
   */
  enableRovingFocus?: boolean;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatMessageList.Root.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatMessageList.Root.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatMessageList.Root.State) => React.CSSProperties | undefined);
};
```

### ChatMessageListRoot.State

```typescript
type ChatMessageListRootState = { messageCount: number; atBottom: boolean };
```

### ChatMessageListStatus

A visually-hidden `role="status"` live region that announces streaming
transitions ("Assistant is responding" / "Response complete") to screen readers.
Renders a `<div>` element.

**ChatMessageListStatus Props:**

| Prop      | Type                                                                                                 | Default | Description                                                                                                                                                                                   |
| :-------- | :--------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageList.Status.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageList.Status.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Status.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatMessageListStatus.Props

```typescript
type ChatMessageListStatusProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatMessageList.Status.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatMessageList.Status.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatMessageList.Status.State) => React.CSSProperties | undefined);
};
```

### ChatMessageListStatus.State

```typescript
type ChatMessageListStatusState = { [key: string]: never };
```

### ChatMessageListViewport

The scrollable viewport of the chat message list.
Bind your scroll container to this element.
Renders a `<div>` element.

**ChatMessageListViewport Props:**

| Prop      | Type                                                                                                   | Default | Description                                                                                                                                                                                   |
| :-------- | :----------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageList.Viewport.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageList.Viewport.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Viewport.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatMessageListViewport.Props

```typescript
type ChatMessageListViewportProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatMessageList.Viewport.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatMessageList.Viewport.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatMessageList.Viewport.State) => React.CSSProperties | undefined);
};
```

### ChatMessageListViewport.State

```typescript
type ChatMessageListViewportState = { [key: string]: never };
```

### DateDivider

A date separator rendered between messages that span a calendar day boundary.
Returns `null` when there is no boundary for the current message.
Renders a `<div>` element.

**DateDivider Props:**

| Prop              | Type                                                                                                                                                   | Default | Description                                                                                                                                                                                   |
| :---------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| formatDate        | `((date: Date) => React.ReactNode)`                                                                                                                    | -       | -                                                                                                                                                                                             |
| index             | `number`                                                                                                                                               | -       | -                                                                                                                                                                                             |
| items             | `string[]`                                                                                                                                             | -       | -                                                                                                                                                                                             |
| messageId\*       | `string`                                                                                                                                               | -       | -                                                                                                                                                                                             |
| shouldShowDivider | `((params: { message: ChatMessage; previousMessage: ChatMessage \| null; index: number; date: Date \| null; previousDate: Date \| null }) => boolean)` | -       | -                                                                                                                                                                                             |
| className         | `string \| ((state: ChatMessageList.DateDivider.State) => string \| undefined)`                                                                        | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style             | `React.CSSProperties \| ((state: ChatMessageList.DateDivider.State) => React.CSSProperties \| undefined)`                                              | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render            | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.DateDivider.State) => ReactElement)`                                                       | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### DateDivider.Props

Re-export of [DateDivider](#datedivider) props.

### DateDivider.State

```typescript
type ChatMessageListDateDividerState = {
  messageId: string;
  hasBoundary: boolean;
  label: React.ReactNode;
};
```

### Overlay

A floating layer rendered above the message list, anchored to its bottom edge.
Pointer events pass through to the list below.
Renders a `<div>` element.

**Overlay Props:**

| Prop      | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageList.Overlay.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageList.Overlay.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageList.Overlay.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Overlay.Props

Re-export of [Overlay](#overlay) props.

### Overlay.State

```typescript
type ChatMessageListOverlayState = { [key: string]: never };
```

### useMessageActionable

**Return Value:**

```tsx
type ReturnValue = boolean;
```

### useMessageContentTabIndex

**Return Value:**

```tsx
type ReturnValue = number | undefined;
```

### useMessageListContext

**Return Value:**

```tsx
type ReturnValue = MessageListContextValue;
```

### useMessageRovingItem

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| messageId | `string` | -       | -           |

**Return Value:**

| Property     | Type      | Description |
| :----------- | :-------- | :---------- |
| `enabled`    | `boolean` | -           |
| `focused`    | `boolean` | -           |
| `actionable` | `boolean` | -           |

## Additional Types

### MessageListRootHandle

```typescript
type MessageListRootHandle = {
  scrollToBottom: scrollToBottom;
  /**
   * Move the roving focus to the given message (defaults to the newest one)
   * and focus its article element.
   */
  focusMessage: focusMessage;
};
```

## External Types

### renderItem

```typescript
type renderItem = (params: { id: string; index: number }) => ReactNode;
```

### scrollToBottom

```typescript
type scrollToBottom = (options?: { behavior?: ScrollBehavior | undefined } | undefined) => void;
```

### focusMessage

```typescript
type focusMessage = (id?: string | undefined) => void;
```

## Export Groups

- `ChatMessageList.Root`: `ChatMessageList.Root`, `ChatMessageList.Root.State`, `ChatMessageList.Root.Props`
- `ChatMessageList.Viewport`: `ChatMessageList.Viewport`, `ChatMessageList.Viewport.State`, `ChatMessageList.Viewport.Props`
- `ChatMessageList.Content`: `ChatMessageList.Content`, `ChatMessageList.Content.State`, `ChatMessageList.Content.Props`
- `ChatMessageList.Overlay`: `ChatMessageList.Overlay`, `ChatMessageList.Overlay.State`, `ChatMessageList.Overlay.Props`
- `ChatMessageList.Status`: `ChatMessageList.Status`, `ChatMessageList.Status.State`, `ChatMessageList.Status.Props`
- `ChatMessageList.DateDivider`: `ChatMessageList.DateDivider`, `ChatMessageList.DateDivider.State`, `ChatMessageList.DateDivider.Props`
- `ChatMessageList.useMessageListContext`
- `ChatMessageList.useMessageActionable`
- `ChatMessageList.useMessageRovingItem`
- `ChatMessageList.useMessageContentTabIndex`
- `Default`: `ChatMessageListRoot`, `ChatMessageListRoot.State`, `ChatMessageListRoot.Props`, `MessageListRootHandle`, `ChatMessageListViewport`, `ChatMessageListViewport.State`, `ChatMessageListViewport.Props`, `ChatMessageListContent`, `ChatMessageListContent.State`, `ChatMessageListContent.Props`, `ChatMessageListOverlay`, `ChatMessageListOverlay.State`, `ChatMessageListOverlay.Props`, `ChatMessageListStatus`, `ChatMessageListStatus.State`, `ChatMessageListStatus.Props`, `ChatMessageListDateDivider`, `ChatMessageListDateDivider.State`, `ChatMessageListDateDivider.Props`

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `ChatMessageList.Root`: `ChatMessageListRoot`
- `ChatMessageList.Viewport`: `ChatMessageListViewport`
- `ChatMessageList.Content`: `ChatMessageListContent`
- `ChatMessageList.Overlay`: `ChatMessageListOverlay`
- `ChatMessageList.Status`: `ChatMessageListStatus`
- `ChatMessageList.DateDivider`: `ChatMessageListDateDivider`
