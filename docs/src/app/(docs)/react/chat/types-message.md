# Chat

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/react/chat/types-message.md"'

## API Reference

### Root

The root element for a single chat message.
Renders a `<div>` element with `role="article"`.

**Root Props:**

| Prop        | Type                                                                               | Default | Description                                                                                                                                                                                   |
| :---------- | :--------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| grouped     | `boolean`                                                                          | -       | -                                                                                                                                                                                             |
| messageId\* | `string`                                                                           | -       | -                                                                                                                                                                                             |
| className   | `string \| ((state: Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style       | `React.CSSProperties \| ((state: Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render      | `ReactElement \| ((props: HTMLProps, state: Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Root.Props

Re-export of [Root](#root) props.

### Root.State

```typescript
type RootState = {
  messageId: string;
  message: ChatMessage | null;
  role?: ChatRole;
  status?: ChatMessageStatus;
  streaming: boolean;
  error: boolean;
  grouped: boolean;
  variant: ChatVariant;
  density: ChatDensity;
  resolvedAuthor: ResolvedMessageAuthor | null;
  showAvatar: boolean;
  ownMessage: boolean;
};
```

### Content

Renders the content of a chat message by dispatching each message part
to the appropriate renderer.
Renders a `<div>` element.

**Content Props:**

| Prop                       | Type                                                                                       | Default | Description                                                                                                                                                                                   |
| :------------------------- | :----------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| afterContent               | `React.ReactNode`                                                                          | -       | -                                                                                                                                                                                             |
| resolveBuiltInPartRenderer | `((part: ChatBuiltInMessagePart, localeText: ChatLocaleText) => ChatPartRenderer \| null)` | -       | -                                                                                                                                                                                             |
| textProps                  | `TextPartProps`                                                                            | -       | -                                                                                                                                                                                             |
| className                  | `string \| ((state: Content.State) => string \| undefined)`                                | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style                      | `React.CSSProperties \| ((state: Content.State) => React.CSSProperties \| undefined)`      | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render                     | `ReactElement \| ((props: HTMLProps, state: Content.State) => ReactElement)`               | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Content.Props

Re-export of [Content](#content) props.

### Content.State

```typescript
type ContentState = {
  messageId: string;
  message: ChatMessage | null;
  role?: ChatRole;
  status?: ChatMessageStatus;
  streaming: boolean;
  error: boolean;
  grouped: boolean;
  variant: ChatVariant;
  density: ChatDensity;
  resolvedAuthor: ResolvedMessageAuthor | null;
  showAvatar: boolean;
  ownMessage: boolean;
};
```

### Error

Renders an error message for the current message.
Returns `null` when there is no error.
Renders a `<div>` element with `role="alert"`.

**Error Props:**

| Prop      | Type                                                                                | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: Error.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: Error.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: Error.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Error.Props

Re-export of [Error](#error) props.

### Error.State

```typescript
type ErrorState = {
  chatError: ChatError | null;
  retryable: boolean;
  retry: () => void | Promise<void>;
  messageId: string;
  message: ChatMessage | null;
  role?: ChatRole;
  status?: ChatMessageStatus;
  streaming: boolean;
  error: boolean;
  grouped: boolean;
  variant: ChatVariant;
  density: ChatDensity;
  resolvedAuthor: ResolvedMessageAuthor | null;
  showAvatar: boolean;
  ownMessage: boolean;
};
```

### Actions

A container for message action buttons.
Renders a `<div>` element.

**Actions Props:**

| Prop      | Type                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------ | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: Actions.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: Actions.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: Actions.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Actions.Props

Re-export of [Actions](#actions) props.

### Actions.State

```typescript
type ActionsState = {
  messageId: string;
  message: ChatMessage | null;
  role?: ChatRole;
  status?: ChatMessageStatus;
  streaming: boolean;
  error: boolean;
  grouped: boolean;
  variant: ChatVariant;
  density: ChatDensity;
  resolvedAuthor: ResolvedMessageAuthor | null;
  showAvatar: boolean;
  ownMessage: boolean;
};
```

### AuthorLabel

Renders the author's display name for a chat message.
Only shown in `compact` variant for the first message in a group.
Renders a `<span>` element.

**AuthorLabel Props:**

| Prop      | Type                                                                                      | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: AuthorLabel.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: AuthorLabel.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: AuthorLabel.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### AuthorLabel.Props

Re-export of [AuthorLabel](#authorlabel) props.

### AuthorLabel.State

```typescript
type AuthorLabelState = {
  messageId: string;
  message: ChatMessage | null;
  role?: ChatRole;
  status?: ChatMessageStatus;
  streaming: boolean;
  error: boolean;
  grouped: boolean;
  variant: ChatVariant;
  density: ChatDensity;
  resolvedAuthor: ResolvedMessageAuthor | null;
  showAvatar: boolean;
  ownMessage: boolean;
};
```

### Avatar

Renders the author's avatar for a chat message.
Returns `null` for system messages, grouped messages, or when no avatar is available.
Renders a `<div>` element.

**Avatar Props:**

| Prop      | Type                                                                                 | Default | Description                                                                                                                                                                                   |
| :-------- | :----------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: Avatar.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: Avatar.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: Avatar.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Avatar.Props

Re-export of [Avatar](#avatar) props.

### Avatar.State

```typescript
type AvatarState = {
  messageId: string;
  message: ChatMessage | null;
  role?: ChatRole;
  status?: ChatMessageStatus;
  streaming: boolean;
  error: boolean;
  grouped: boolean;
  variant: ChatVariant;
  density: ChatDensity;
  resolvedAuthor: ResolvedMessageAuthor | null;
  showAvatar: boolean;
  ownMessage: boolean;
};
```

### Meta

Renders metadata for a chat message: timestamp, status, and streaming indicator.
Returns `null` when there is nothing to display.
Renders a `<div>` element.

**Meta Props:**

| Prop      | Type                                                                               | Default | Description                                                                                                                                                                                   |
| :-------- | :--------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: Meta.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: Meta.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: Meta.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Meta.Props

Re-export of [Meta](#meta) props.

### Meta.State

```typescript
type MetaState = {
  messageId: string;
  message: ChatMessage | null;
  role?: ChatRole;
  status?: ChatMessageStatus;
  streaming: boolean;
  error: boolean;
  grouped: boolean;
  variant: ChatVariant;
  density: ChatDensity;
  resolvedAuthor: ResolvedMessageAuthor | null;
  showAvatar: boolean;
  ownMessage: boolean;
};
```

## External Types

### ChatRole

```typescript
type ChatRole = 'system' | 'user' | 'assistant';
```

### ChatMessageStatus

```typescript
type ChatMessageStatus =
  | 'pending'
  | 'sending'
  | 'streaming'
  | 'sent'
  | 'read'
  | 'error'
  | 'cancelled';
```

### ChatVariant

```typescript
type ChatVariant = 'default' | 'compact';
```

### ChatDensity

```typescript
type ChatDensity = 'compact' | 'standard' | 'comfortable';
```

### ChatPartRenderer

```typescript
type ChatPartRenderer = (props: {
  part:
    | { type: 'text'; text: string; state?: 'streaming' | 'done' | undefined }
    | { type: 'reasoning'; text: string; state?: 'streaming' | 'done' | undefined }
    | { type: 'file'; mediaType: string; url: string; filename?: string | undefined }
    | { type: 'source-url'; sourceId: string; url: string; title?: string | undefined }
    | {
        type: 'source-document';
        sourceId: string;
        title?: string | undefined;
        text?: string | undefined;
      }
    | { type: any; id?: string | undefined; data: unknown; transient?: boolean | undefined }
    | { type: 'step-start' }
    | {
        type: 'tool';
        toolInvocation: {
          toolCallId: string;
          toolName: string;
          state:
            | 'input-streaming'
            | 'input-available'
            | 'approval-requested'
            | 'approval-responded'
            | 'output-available'
            | 'output-error'
            | 'output-denied';
          input?: unknown;
          output?: unknown;
          errorText?: string | undefined;
          approval?: { approved: boolean; reason?: string | undefined } | undefined;
          approvalId?: string | undefined;
          providerExecuted?: boolean | undefined;
          title?: string | undefined;
          callProviderMetadata?: Record | undefined;
          preliminary?: boolean | undefined;
        };
      }
    | {
        type: 'dynamic-tool';
        toolInvocation: {
          toolCallId: string;
          toolName: string;
          state:
            | 'input-streaming'
            | 'input-available'
            | 'approval-requested'
            | 'approval-responded'
            | 'output-available'
            | 'output-error'
            | 'output-denied';
          input?: unknown;
          output?: unknown;
          errorText?: string | undefined;
          approval?: { approved: boolean; reason?: string | undefined } | undefined;
          approvalId?: string | undefined;
          providerExecuted?: boolean | undefined;
          title?: string | undefined;
          callProviderMetadata?: Record | undefined;
          preliminary?: boolean | undefined;
        };
      };
  message: {
    id: string;
    conversationId?: string | undefined;
    role: 'system' | 'user' | 'assistant';
    parts: unknown;
    metadata?: {} | undefined;
    createdAt?: string | undefined;
    updatedAt?: string | undefined;
    status?:
      | 'pending'
      | 'sending'
      | 'streaming'
      | 'sent'
      | 'read'
      | 'error'
      | 'cancelled'
      | undefined;
    author?:
      | {
          id: string;
          displayName?: string | undefined;
          avatarUrl?: string | undefined;
          isOnline?: boolean | undefined;
          role?: 'system' | 'user' | 'assistant' | undefined;
          metadata?: {} | undefined;
        }
      | undefined;
    editedAt?: string | undefined;
  };
  index: number;
  onToolCall?: unknown | undefined;
}) => ReactNode;
```
