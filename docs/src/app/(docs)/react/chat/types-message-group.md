# Chat

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/react/chat/types-message-group.md"'

## API Reference

### Root

Groups consecutive messages from the same author.
Renders a `<div>` element.

**Root Props:**

| Prop        | Type                                                                                                | Default | Description                                                                                                                                                                                   |
| :---------- | :-------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| groupKey    | `GroupKeyFn`                                                                                        | -       | -                                                                                                                                                                                             |
| index       | `number`                                                                                            | -       | -                                                                                                                                                                                             |
| items       | `string[]`                                                                                          | -       | -                                                                                                                                                                                             |
| messageId\* | `string`                                                                                            | -       | -                                                                                                                                                                                             |
| className   | `string \| ((state: ChatMessageGroup.Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style       | `React.CSSProperties \| ((state: ChatMessageGroup.Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render      | `ReactElement \| ((props: HTMLProps, state: ChatMessageGroup.Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Root.Props

Re-export of [Root](#root) props.

### Root.State

```typescript
type ChatMessageGroupRootState = {
  first: boolean;
  firstInList: boolean;
  last: boolean;
  authorRole?: ChatRole;
  authorId?: string;
  ownMessage: boolean;
  variant: ChatVariant;
  density: ChatDensity;
};
```

### AuthorName

Displays the author name for the first message in a group.
Renders nothing when the message is not the first in its group.
Renders a `<div>` element.

**AuthorName Props:**

| Prop      | Type                                                                                                      | Default | Description                                                                                                                                                                                   |
| :-------- | :-------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageGroup.AuthorName.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageGroup.AuthorName.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageGroup.AuthorName.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### AuthorName.Props

Re-export of [AuthorName](#authorname) props.

### AuthorName.State

```typescript
type ChatMessageGroupAuthorNameState = {
  first: boolean;
  firstInList: boolean;
  last: boolean;
  authorRole?: ChatRole;
  authorId?: string;
  ownMessage: boolean;
  variant: ChatVariant;
  density: ChatDensity;
};
```

### ChatMessageGroupAuthorName

Displays the author name for the first message in a group.
Renders nothing when the message is not the first in its group.
Renders a `<div>` element.

**ChatMessageGroupAuthorName Props:**

| Prop      | Type                                                                                                      | Default | Description                                                                                                                                                                                   |
| :-------- | :-------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatMessageGroup.AuthorName.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatMessageGroup.AuthorName.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatMessageGroup.AuthorName.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatMessageGroupAuthorName.Props

```typescript
type ChatMessageGroupAuthorNameProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatMessageGroup.AuthorName.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatMessageGroup.AuthorName.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatMessageGroup.AuthorName.State) => React.CSSProperties | undefined);
};
```

### ChatMessageGroupAuthorName.State

```typescript
type ChatMessageGroupAuthorNameState = {
  first: boolean;
  firstInList: boolean;
  last: boolean;
  authorRole?: ChatRole;
  authorId?: string;
  ownMessage: boolean;
  variant: ChatVariant;
  density: ChatDensity;
};
```

### ChatMessageGroupRoot

Groups consecutive messages from the same author.
Renders a `<div>` element.

**ChatMessageGroupRoot Props:**

| Prop        | Type                                                                                                | Default | Description                                                                                                                                                                                   |
| :---------- | :-------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| groupKey    | `GroupKeyFn`                                                                                        | -       | -                                                                                                                                                                                             |
| index       | `number`                                                                                            | -       | -                                                                                                                                                                                             |
| items       | `string[]`                                                                                          | -       | -                                                                                                                                                                                             |
| messageId\* | `string`                                                                                            | -       | -                                                                                                                                                                                             |
| className   | `string \| ((state: ChatMessageGroup.Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style       | `React.CSSProperties \| ((state: ChatMessageGroup.Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render      | `ReactElement \| ((props: HTMLProps, state: ChatMessageGroup.Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatMessageGroupRoot.Props

```typescript
type ChatMessageGroupRootProps = {
  messageId: string;
  index?: number;
  items?: string[];
  groupKey?: GroupKeyFn;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatMessageGroup.Root.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatMessageGroup.Root.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatMessageGroup.Root.State) => React.CSSProperties | undefined);
};
```

### ChatMessageGroupRoot.State

```typescript
type ChatMessageGroupRootState = {
  first: boolean;
  firstInList: boolean;
  last: boolean;
  authorRole?: ChatRole;
  authorId?: string;
  ownMessage: boolean;
  variant: ChatVariant;
  density: ChatDensity;
};
```

### createTimeWindowGroupKey

Creates a `groupKey` function that groups messages by author within a sliding
time window.

**Parameters:**

| Parameter | Type     | Default  | Description |
| :-------- | :------- | :------- | :---------- |
| windowMs? | `number` | `300000` | -           |

**Return Value:**

```tsx
type ReturnValue = GroupKeyFn;
```

### GroupKeyFn

A function that maps a message to a group key.
Messages that resolve to the same key are visually grouped together.

**Parameters:**

| Parameter | Type          | Default | Description |
| :-------- | :------------ | :------ | :---------- |
| message   | `ChatMessage` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string | number;
```

## External Types

### ChatRole

```typescript
type ChatRole = 'system' | 'user' | 'assistant';
```

### ChatVariant

```typescript
type ChatVariant = 'default' | 'compact';
```

### ChatDensity

```typescript
type ChatDensity = 'compact' | 'standard' | 'comfortable';
```

## Export Groups

- `ChatMessageGroup.Root`: `ChatMessageGroup.Root`, `ChatMessageGroup.Root.State`, `ChatMessageGroup.Root.Props`
- `ChatMessageGroup.AuthorName`: `ChatMessageGroup.AuthorName`, `ChatMessageGroup.AuthorName.State`, `ChatMessageGroup.AuthorName.Props`
- `ChatMessageGroup.createTimeWindowGroupKey`
- `Default`: `ChatMessageGroupRoot`, `ChatMessageGroupRoot.State`, `ChatMessageGroupRoot.Props`, `ChatMessageGroupAuthorName`, `ChatMessageGroupAuthorName.State`, `ChatMessageGroupAuthorName.Props`, `GroupKeyFn`

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `ChatMessageGroup.Root`: `ChatMessageGroupRoot`
- `ChatMessageGroup.AuthorName`: `ChatMessageGroupAuthorName`
