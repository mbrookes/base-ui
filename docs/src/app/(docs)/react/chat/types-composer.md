# Chat

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/react/chat/types-composer.md"'

## API Reference

### Root

The root form element for the chat composer.
Renders a `<form>` element.

**Root Props:**

| Prop             | Type                                                                                            | Default | Description                                                                                                                                                                                   |
| :--------------- | :---------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| attachmentConfig | `ChatAttachmentsConfig`                                                                         | -       | -                                                                                                                                                                                             |
| onSubmit         | `React.FormEventHandler<HTMLFormElement>`                                                       | -       | -                                                                                                                                                                                             |
| disabled         | `boolean`                                                                                       | -       | -                                                                                                                                                                                             |
| className        | `string \| ((state: ChatComposer.Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style            | `React.CSSProperties \| ((state: ChatComposer.Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render           | `ReactElement \| ((props: HTMLProps, state: ChatComposer.Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Root.Props

Re-export of [Root](#root) props.

### Root.State

```typescript
type ChatComposerRootState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### Label

A `<label>` element for the conversation input textarea.
Renders a `<label>` element.

**Label Props:**

| Prop      | Type                                                                                             | Default | Description                                                                                                                                                                                   |
| :-------- | :----------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.Label.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.Label.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.Label.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Label.Props

Re-export of [Label](#label) props.

### Label.State

```typescript
type ChatComposerLabelState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### AttachButton

A button that opens a file picker to attach files to the message.
Renders a `<button>` element.

**AttachButton Props:**

| Prop      | Type                                                                                                    | Default | Description                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------------ | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.AttachButton.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.AttachButton.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.AttachButton.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### AttachButton.Props

Re-export of [AttachButton](#attachbutton) props.

### AttachButton.State

```typescript
type ChatComposerAttachButtonState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### AttachmentList

Renders the list of pending file attachments.
Returns `null` when there are no attachments.
Renders a `<div>` element.

**AttachmentList Props:**

| Prop      | Type                                                                                                      | Default | Description                                                                                                                                                                                   |
| :-------- | :-------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.AttachmentList.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.AttachmentList.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.AttachmentList.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### AttachmentList.Props

Re-export of [AttachmentList](#attachmentlist) props.

### AttachmentList.State

```typescript
type ChatComposerAttachmentListState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### ChatComposerAttachButton

A button that opens a file picker to attach files to the message.
Renders a `<button>` element.

**ChatComposerAttachButton Props:**

| Prop      | Type                                                                                                    | Default | Description                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------------ | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.AttachButton.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.AttachButton.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.AttachButton.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatComposerAttachButton.Props

```typescript
type ChatComposerAttachButtonProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatComposer.AttachButton.State) => string | undefined);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatComposer.AttachButton.State) => React.CSSProperties | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatComposer.AttachButton.State) => ReactElement);
};
```

### ChatComposerAttachButton.State

```typescript
type ChatComposerAttachButtonState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### ChatComposerAttachmentList

Renders the list of pending file attachments.
Returns `null` when there are no attachments.
Renders a `<div>` element.

**ChatComposerAttachmentList Props:**

| Prop      | Type                                                                                                      | Default | Description                                                                                                                                                                                   |
| :-------- | :-------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.AttachmentList.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.AttachmentList.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.AttachmentList.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatComposerAttachmentList.Props

```typescript
type ChatComposerAttachmentListProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatComposer.AttachmentList.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatComposer.AttachmentList.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatComposer.AttachmentList.State) => React.CSSProperties | undefined);
};
```

### ChatComposerAttachmentList.State

```typescript
type ChatComposerAttachmentListState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### ChatComposerHelperText

Renders helper text or error messages for the composer.
Returns `null` when there is no content to display.
Renders a `<div>` element.

**ChatComposerHelperText Props:**

| Prop      | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.HelperText.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.HelperText.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.HelperText.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatComposerHelperText.Props

```typescript
type ChatComposerHelperTextProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatComposer.HelperText.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatComposer.HelperText.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatComposer.HelperText.State) => React.CSSProperties | undefined);
};
```

### ChatComposerHelperText.State

```typescript
type ChatComposerHelperTextState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
  error: boolean;
};
```

### ChatComposerLabel

A `<label>` element for the conversation input textarea.
Renders a `<label>` element.

**ChatComposerLabel Props:**

| Prop      | Type                                                                                             | Default | Description                                                                                                                                                                                   |
| :-------- | :----------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.Label.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.Label.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.Label.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatComposerLabel.Props

```typescript
type ChatComposerLabelProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatComposer.Label.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatComposer.Label.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatComposer.Label.State) => React.CSSProperties | undefined);
};
```

### ChatComposerLabel.State

```typescript
type ChatComposerLabelState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### ChatComposerRoot

The root form element for the chat composer.
Renders a `<form>` element.

**ChatComposerRoot Props:**

| Prop             | Type                                                                                            | Default | Description                                                                                                                                                                                   |
| :--------------- | :---------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| attachmentConfig | `ChatAttachmentsConfig`                                                                         | -       | -                                                                                                                                                                                             |
| onSubmit         | `React.FormEventHandler<HTMLFormElement>`                                                       | -       | -                                                                                                                                                                                             |
| disabled         | `boolean`                                                                                       | -       | -                                                                                                                                                                                             |
| className        | `string \| ((state: ChatComposer.Root.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style            | `React.CSSProperties \| ((state: ChatComposer.Root.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render           | `ReactElement \| ((props: HTMLProps, state: ChatComposer.Root.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatComposerRoot.Props

```typescript
type ChatComposerRootProps = {
  onSubmit?: React.FormEventHandler<HTMLFormElement>;
  disabled?: boolean;
  attachmentConfig?: ChatAttachmentsConfig;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatComposer.Root.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatComposer.Root.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatComposer.Root.State) => React.CSSProperties | undefined);
};
```

### ChatComposerRoot.State

```typescript
type ChatComposerRootState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### ChatComposerSendButton

The send button for the chat composer.
Renders a `<button>` element.

**ChatComposerSendButton Props:**

| Prop      | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.SendButton.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.SendButton.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.SendButton.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatComposerSendButton.Props

```typescript
type ChatComposerSendButtonProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatComposer.SendButton.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: ChatComposer.SendButton.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatComposer.SendButton.State) => React.CSSProperties | undefined);
};
```

### ChatComposerSendButton.State

```typescript
type ChatComposerSendButtonState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### ChatComposerTextArea

The text input area for the chat composer.
Renders a `<textarea>` element with auto-resize behavior.

**ChatComposerTextArea Props:**

| Prop               | Type                                                                                                | Default | Description                                                                                                                                                                                   |
| :----------------- | :-------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| onCompositionEnd   | `React.CompositionEventHandler<HTMLTextAreaElement>`                                                | -       | -                                                                                                                                                                                             |
| onCompositionStart | `React.CompositionEventHandler<HTMLTextAreaElement>`                                                | -       | -                                                                                                                                                                                             |
| onKeyDown          | `React.KeyboardEventHandler<HTMLTextAreaElement>`                                                   | -       | -                                                                                                                                                                                             |
| className          | `string \| ((state: ChatComposer.TextArea.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style              | `React.CSSProperties \| ((state: ChatComposer.TextArea.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render             | `ReactElement \| ((props: HTMLProps, state: ChatComposer.TextArea.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatComposerTextArea.Props

```typescript
type ChatComposerTextAreaProps = {
  onKeyDown?: React.KeyboardEventHandler<HTMLTextAreaElement>;
  onCompositionStart?: React.CompositionEventHandler<HTMLTextAreaElement>;
  onCompositionEnd?: React.CompositionEventHandler<HTMLTextAreaElement>;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatComposer.TextArea.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatComposer.TextArea.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatComposer.TextArea.State) => React.CSSProperties | undefined);
};
```

### ChatComposerTextArea.State

```typescript
type ChatComposerTextAreaState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### ChatComposerToolbar

A container for composer action buttons.
Renders a `<div>` element.

**ChatComposerToolbar Props:**

| Prop      | Type                                                                                               | Default | Description                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.Toolbar.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.Toolbar.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.Toolbar.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### ChatComposerToolbar.Props

```typescript
type ChatComposerToolbarProps = {
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: ChatComposer.Toolbar.State) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: ChatComposer.Toolbar.State) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: ChatComposer.Toolbar.State) => React.CSSProperties | undefined);
};
```

### ChatComposerToolbar.State

```typescript
type ChatComposerToolbarState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### HelperText

Renders helper text or error messages for the composer.
Returns `null` when there is no content to display.
Renders a `<div>` element.

**HelperText Props:**

| Prop      | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.HelperText.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.HelperText.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.HelperText.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### HelperText.Props

Re-export of [HelperText](#helpertext) props.

### HelperText.State

```typescript
type ChatComposerHelperTextState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
  error: boolean;
};
```

### SendButton

The send button for the chat composer.
Renders a `<button>` element.

**SendButton Props:**

| Prop      | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :-------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.SendButton.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.SendButton.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.SendButton.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### SendButton.Props

Re-export of [SendButton](#sendbutton) props.

### SendButton.State

```typescript
type ChatComposerSendButtonState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### TextArea

The text input area for the chat composer.
Renders a `<textarea>` element with auto-resize behavior.

**TextArea Props:**

| Prop               | Type                                                                                                | Default | Description                                                                                                                                                                                   |
| :----------------- | :-------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| onCompositionEnd   | `React.CompositionEventHandler<HTMLTextAreaElement>`                                                | -       | -                                                                                                                                                                                             |
| onCompositionStart | `React.CompositionEventHandler<HTMLTextAreaElement>`                                                | -       | -                                                                                                                                                                                             |
| onKeyDown          | `React.KeyboardEventHandler<HTMLTextAreaElement>`                                                   | -       | -                                                                                                                                                                                             |
| className          | `string \| ((state: ChatComposer.TextArea.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style              | `React.CSSProperties \| ((state: ChatComposer.TextArea.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render             | `ReactElement \| ((props: HTMLProps, state: ChatComposer.TextArea.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### TextArea.Props

Re-export of [TextArea](#textarea) props.

### TextArea.State

```typescript
type ChatComposerTextAreaState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### Toolbar

A container for composer action buttons.
Renders a `<div>` element.

**Toolbar Props:**

| Prop      | Type                                                                                               | Default | Description                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| className | `string \| ((state: ChatComposer.Toolbar.State) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style     | `React.CSSProperties \| ((state: ChatComposer.Toolbar.State) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render    | `ReactElement \| ((props: HTMLProps, state: ChatComposer.Toolbar.State) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

### Toolbar.Props

Re-export of [Toolbar](#toolbar) props.

### Toolbar.State

```typescript
type ChatComposerToolbarState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### useComposerContext

**Return Value:**

```tsx
type ReturnValue = ComposerContextValue;
```

## Additional Types

### ComposerContextValue

```typescript
type ComposerContextValue = {
  value: string;
  setValue: setValue;
  submit: submit;
  addAttachment: addAttachment;
  removeAttachment: removeAttachment;
  attachments: ChatDraftAttachment[];
  attachmentConfig?: ChatAttachmentsConfig;
  error: ChatError | null;
  setComposerIsComposing: setComposerIsComposing;
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

### ComposerState

```typescript
type ComposerState = {
  submitting: boolean;
  hasValue: boolean;
  streaming: boolean;
  attachmentCount: number;
  disabled: boolean;
};
```

## External Types

### setValue

```typescript
type setValue = (value: string) => void;
```

### submit

```typescript
type submit = () => Promise;
```

### addAttachment

```typescript
type addAttachment = (file: File) => void;
```

### removeAttachment

```typescript
type removeAttachment = (localId: string) => void;
```

### setComposerIsComposing

```typescript
type setComposerIsComposing = (value: boolean) => void;
```

## Export Groups

- `ChatComposer.Root`: `ChatComposer.Root`, `ChatComposer.Root.State`, `ChatComposer.Root.Props`
- `ChatComposer.TextArea`: `ChatComposer.TextArea`, `ChatComposer.TextArea.State`, `ChatComposer.TextArea.Props`
- `ChatComposer.SendButton`: `ChatComposer.SendButton`, `ChatComposer.SendButton.State`, `ChatComposer.SendButton.Props`
- `ChatComposer.AttachButton`: `ChatComposer.AttachButton`, `ChatComposer.AttachButton.State`, `ChatComposer.AttachButton.Props`
- `ChatComposer.AttachmentList`: `ChatComposer.AttachmentList`, `ChatComposer.AttachmentList.State`, `ChatComposer.AttachmentList.Props`
- `ChatComposer.Toolbar`: `ChatComposer.Toolbar`, `ChatComposer.Toolbar.State`, `ChatComposer.Toolbar.Props`
- `ChatComposer.HelperText`: `ChatComposer.HelperText`, `ChatComposer.HelperText.State`, `ChatComposer.HelperText.Props`
- `ChatComposer.Label`: `ChatComposer.Label`, `ChatComposer.Label.State`, `ChatComposer.Label.Props`
- `ChatComposer.useComposerContext`
- `Default`: `ChatComposerRoot`, `ChatComposerRoot.State`, `ChatComposerRoot.Props`, `ChatComposerTextArea`, `ChatComposerTextArea.State`, `ChatComposerTextArea.Props`, `ChatComposerSendButton`, `ChatComposerSendButton.State`, `ChatComposerSendButton.Props`, `ChatComposerAttachButton`, `ChatComposerAttachButton.State`, `ChatComposerAttachButton.Props`, `ChatComposerAttachmentList`, `ChatComposerAttachmentList.State`, `ChatComposerAttachmentList.Props`, `ChatComposerToolbar`, `ChatComposerToolbar.State`, `ChatComposerToolbar.Props`, `ChatComposerHelperText`, `ChatComposerHelperText.State`, `ChatComposerHelperText.Props`, `ChatComposerLabel`, `ChatComposerLabel.State`, `ChatComposerLabel.Props`, `ComposerState`, `ComposerContextValue`

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `ChatComposer.Root`: `ChatComposerRoot`
- `ChatComposer.TextArea`: `ChatComposerTextArea`
- `ChatComposer.SendButton`: `ChatComposerSendButton`
- `ChatComposer.AttachButton`: `ChatComposerAttachButton`
- `ChatComposer.AttachmentList`: `ChatComposerAttachmentList`
- `ChatComposer.Toolbar`: `ChatComposerToolbar`
- `ChatComposer.HelperText`: `ChatComposerHelperText`
- `ChatComposer.Label`: `ChatComposerLabel`
